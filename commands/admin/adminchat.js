// reload.js File
// Code by Hùng Channels
// #Discord.js #HungChannels #Code
const { ownerid } = require('../../config.json');
module.exports = {
    name: `adminchat`,
    aliases: [`adminchat`],
    category: `admin`,
    description: `Chat từ discord vào 2y2c`,
    usage: `${process.env.DISCORD_PREFIX}adminchat <nội dung>`,
    run: async (client, message, args) => {
        return
    }
}